version: "3.9"
services:
  invhub-account-backend:
    container_name: "invhub-account-backend"
    image: "invhub-account-backend:${APP_VER}"
    build:
      context: .
      dockerfile: .docker/Dockerfile
    env_file:
      - .env
    restart: unless-stopped
    networks:
      - invhub
networks:
  invhub:
    external: true